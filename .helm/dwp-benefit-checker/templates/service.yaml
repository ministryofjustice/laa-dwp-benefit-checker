apiVersion: v1
kind: Service
metadata:
  name: {{ include "dwp-benefit-checker.fullname" . }}
  labels:
    {{- include "dwp-benefit-checker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dwp-benefit-checker.selectorLabels" . | nindent 4 }}